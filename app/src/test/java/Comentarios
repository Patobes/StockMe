----------Pendientes:--------------------------------------------------------------
-Navegacion: al volver de StockAdd regresa a Listas en vez de a Stock;
             al volver de ArticuloSimpleAdd (llamado desde Fragment_catalogo_tipos) vuelve al Fragment_catalogo_todos
-Navegacion: al llamar a Catalogo desde StockAdd o desde ArticulosAdd y darle a volver te dice si quieres cerrar la app

-Depurar las listas añadiendo patrones ViewHolder, etc...

----------Ideas:-------------------------------------------------------------------
en los Fragments se puede usar getActivity() para trabajar sobre el padre

----------Realizado recientemente:-------------------------------------------------
-Autocompletado de marcas
-Vista de bd
-Modificar elementos de la lista de la compra como el precio de un artículo
-Cambiar comportamiento lista compra para usar los checkboxes

---------Tobes/Jaun Rama master---------------------------------------------------
-Corregir la lista de ListaDeArticulos. El botón se desplaza al insertar elemento.
-Poner valore por defecto en la bd
-Revisar comoprtamiento si no se insertan campos en artículos_add
-Suma de precio en ListaDeArticulos (quizá renombrar a lista de compra)


----------Stock--------------
- Darle estilo acorde al diseño de toda la app (+-)
- if(cantidad == 0) {DialogInterface(¿conservar o eliminar del Stock?)}
- if(cantidad < minimo) {DialogInterface(¿añadir a lista de la compra/a qué lista de la compra desea añadirlo?)}
- Cambiar color de fondo segun estado [0<cantidad<minimo | 0<=minimo<cantidad | 0 == cantidad == minimo | etc] | [esta en lista de compra o no]
